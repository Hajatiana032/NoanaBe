{% extends "admin/layout.html.twig" %}
{% block title %}{{ parent() }}-Admin(catégories){% endblock %}

{% block stylesheets %}
{% endblock %}

{% block main %}
<div class="container">
    <h1 class="text-cursive fw-bold">Liste des catégories</h1>
    {% include "partials/_flashMessages.html.twig" %}
    <a href="{{ path('admin_category_new') }}" class="btn btn-primary mb-2">
        <i class="fa fa-add">&nbsp;</i>
        Ajouter une catégorie
    </a>
    <div class="table-responsive">
        <table class="table table-bordered text-nowrap">
            <thead>
                <tr>
                    <th class="col-6">Nom</th>
                    <th class="col-6">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for category in categories %}
                <tr>
                    <td>{{ category.name }}</td>
                    <td>
                        <a href="{{ path('admin_category_edit', {slug: category.slug}) }}" class="btn border-0"
                            data-bs-toggle="tooltip" title="Modifier">
                            <i class="fa fa-edit text-warning"></i>
                        </a>
                        <button class="btn border-0" data-bs-toggle="modal" data-bs-target="#{{ category.id }}">
                            <i class="fa fa-trash text-danger"></i>
                        </button>
                        {{ component('DeleteModal', {id: category.id, title: 'Catégorie: ' ~ category.name, message:
                        'Voulez-vous
                        supprimer la catégorie ' ~
                        category.name ~ '?', path: path('admin_category_delete', {slug: category.slug}) }) }}
                    </td>
                </tr>
                {% else %}
                <tr class="text-center">
                    <td colspan="2">
                        <div class="container">
                            <div class="row">
                                <div class="placeholder-wave col-lg-4">
                                    <span class="placeholder placeholder-xs col-12"></span><br>
                                    <span class="placeholder col-12"></span>
                                </div>
                                <div class="col-lg-4">
                                    <span class="text-danger">Aucun résultat</span>
                                </div>
                                <div class="placeholder-wave col-lg-4">
                                    <span class="placeholder placeholder-xs col-12"></span><br>
                                    <span class="placeholder col-12"></span>
                                </div>
                            </div>
                        </div>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}