{% extends 'admin/layout.html.twig' %}

{% block title %}{{ parent() }}-Admin(produits){% endblock %}

{% block main %}
<div class="container">
    <h1 class="text-cursive fw-bold">Modifications de la catégorie "{{ category.name }}"</h1>
    <a href="{{ path('admin_category') }}" class="link-danger text-decoration-none">
        <i class="fa fa-arrow-alt-circle-left">&nbsp;</i>
        Annuler
    </a>
    <a href="{{ path('admin_product') }}" class="link-danger text-decoration-none">
        <i class="fa fa-arrow-alt-circle-left">&nbsp;</i>
        Annuler
    </a>

    {{ include('admin/product/_form.html.twig', {'button_label': 'Update'}) }}
</div>

{% endblock %}